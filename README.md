Basic configuration for `Gulp`.

Commands:
 - `npm start` - running dev-server;
 - `npm run build` - builds the app for production to the `dist` folder;
 - `npm run test` - runs `Stylelint` and `ESLint` checks for the `src` folder;
